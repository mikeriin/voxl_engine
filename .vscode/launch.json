{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "LLDB: debug target (CMake Tools)",
      "type": "lldb",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "cwd": "${command:cmake.getLaunchTargetDirectory}",
      "args": []
    }
  ]
}